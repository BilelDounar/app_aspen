import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Map() {
    return (
        <View className="flex-1 bg-red-500 mt-8">
            <Text className="text-2xl font-bold">Map</Text>
        </View>
    );
}
